name: report-exporter
description: "Bloop tool: report-exporter"
input_schema:
  type: object
  properties:
    input:
      type: string
      description: "Input data path"
  required:
    - input
implementation:
  type: script
  runtime: node
  path: tools/report-exporter.js
