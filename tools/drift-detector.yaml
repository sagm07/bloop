name: drift-detector
description: "Bloop tool: drift-detector"
input_schema:
  type: object
  properties:
    input:
      type: string
      description: "Input data path"
  required:
    - input
implementation:
  type: script
  runtime: node
  path: tools/drift-detector.js
