name: bias-auditor
description: "Detects demographic parity and equalized odds gaps across subgroups"
input_schema:
  type: object
  properties:
    metrics_file:
      type: string
      description: "Path to CSV with segment-level metrics"
  required:
    - metrics_file
implementation:
  type: script
  runtime: node
  path: tools/bias-auditor.js