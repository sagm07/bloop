name: csv-loader
description: "Bloop tool: csv-loader"
input_schema:
  type: object
  properties:
    input:
      type: string
      description: "Input data path"
  required:
    - input
implementation:
  type: script
  runtime: node
  path: tools/csv-loader.js
